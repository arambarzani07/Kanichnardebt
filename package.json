{
  "name": "kanichnar-debt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "d1:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "d1:migrate:local": "wrangler d1 migrations apply DB --local",
    "d1:migrate:create": "wrangler d1 migrations create DB init_debt_system"
  },
  "dependencies": {
    "wrangler": "^4.59.2"
  }
}